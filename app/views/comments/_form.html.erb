<strong>New comment</strong>
<%= form_for [@ticket, @comment], :html => {:class=> "form_stacked" } do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <p>
  <%= f.label :text %> <br/>
  <%= f.text_area :text %> 
  </p>
  <%= f.submit %> 
<% end %>
