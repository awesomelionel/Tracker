<%= admins_only do %>
<%= link_to "Create New Project", new_project_path, :class => "btn primary large" %>
<%= link_to "Admin Page", admin_root_path, :class => "btn primary large" %>
<% end %>

<h2> Projects </h2>
<ul>
	<% @project.each do |project| %>
		<li><%= link_to project.name, project %></li>
	<%end%>
</ul>
