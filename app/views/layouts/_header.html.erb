<!--TODO Project path --> 
<%=link_to "Project name", '#', :class => "brand" %> 
<ul class="nav">
  <li><%=link_to "Home", root_path, :class => "active" %></li>
  <li><%=link_to "Help", help_path, :class => "active" %></li>
  <li><%=link_to "About", about_path, :class => "active" %></li>
  <li><%=link_to "Contact", contact_path, :class => "active" %></li>
  <%if user_signed_in? %>
    <li><%=link_to "Projects", projects_path, :class => "active" %></li>
  <% else %>
  <% end%>
</ul>
<% if user_signed_in? %>
  <p class="pull-right">Logged in as 
  <%= link_to  current_user %> 
  <%= link_to "Sign out", destroy_user_session_path, :class => "active" %>
  </p>
<% else %>
  <p class="pull-right"><%=link_to "Sign in", new_user_session_path, :class => "active" %></p>
<% end %>
